// Stub implementation for natural
export class TfIdf {
  addDocument(text: string): void {
    // Stub implementation
  }

  tfidfs(term: string, callback: (i: number, measure: number) => void): void {
    // Stub implementation - return 0 for all documents
    callback(0, 0);
  }
}
